/*
 para, faça 
 contador; condição; manipulação do contador (incremento / decremento)

*/

const quantidadeCalculada = 3

for(let quantidadePetisco = 1; quantidadePetisco <= quantidadeCalculada; quantidadePetisco++) {
    console.log(`Dando petisco de n° ${quantidadePetisco}`) 

    //Dentro do for declara e inicializa a variavel, coloca a condição e incrementa.
    //quando o incremento atinge a quantidade passada por parâmetro, no caso 3, ele continua fazendo a condição.
}

const gatos = ["Alice" , "Ted", "Nick"] //indice

for(let indice = 0; indice < gatos.length; indice++) {
    console.log(`Dando peticos para: ${gatos[indice]}`)

}

